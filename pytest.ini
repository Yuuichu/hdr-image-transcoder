[pytest]
testpaths = tests
addopts = -m "not fidelity"
markers =
    quick: fast tests that do not perform full encode/decode fidelity conversions
    fidelity: slower end-to-end fidelity tests that encode and decode output images
    tools: tests that require bundled command-line tools
    gui: Electron renderer/main-process wiring checks
